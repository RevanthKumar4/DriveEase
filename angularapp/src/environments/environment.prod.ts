// Production environment — Vercel frontend + Render backend
// Replace the URL below with your actual Render backend URL after deployment
// Example: https://driveease-backend.onrender.com
export const environment = {
  production: true,
  apiURL: 'https://driveease-backend.onrender.com'  // ← UPDATE THIS after Render deploy
};
